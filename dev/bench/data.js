{
  "lastUpdate": 1788284185097,
  "repoUrl": "https://github.com/autumn-foundation/AletheiaDB",
  "entries": {
    "AletheiaDB Benchmarks": [
      {
        "commit": {
          "author": {
            "email": "markmasterson@gmail.com",
            "name": "Mark Masterson",
            "username": "madmax983"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "c20803a059899972c39fca0ac02ee1d6a6cc0f9c",
          "message": "Background adjacency maintenance: make the frozen-CSR fast path reachable, and fix the torn reads that exposes (#3810) (#3812)\n\nCloses #3810.\n\nADR-0026 designed a two-tier adjacency index whose read fast path is\nreachable\n**only when the delta buffer and tombstone set are globally empty** — a\nstate\nonly `compact()` produces. Phase 5 shipped a `CompactionScheduler` that\nno\nshipping constructor ever started, so compaction never ran in a real\ndatabase,\nfor any graph size, for the life of the process, and **100% of adjacency\nreads\npermanently took the merged (delta) path**.\n\nWiring it up was the easy half. Starting compaction makes seven latent\ndefects\nreachable, one of which returns an **empty** adjacency list. All seven\nare fixed\nhere, each with a test covering it (six are red without their fix; the\nseventh\nguards a method this PR introduces, so there is no \"before\" to be red\nagainst --\nthe table below says which is which).\n\n## Approach\n\nRed → green → refactor. Planning used brainstorming, reverse\nbrainstorming and\nsix-hats; the decision record is\n[ADR-0060](docs/adr/0060-background-adjacency-maintenance.md).\n\n**One shared, process-wide worker**, not the per-index scheduler\nADR-0026\nshipped. A database owns two adjacency indexes and the test suite alone\nbuilds\nhundreds of ephemeral databases, so per-index threads meant 2\nthreads/database, a\n`Drop`/shutdown obligation on every owner, and teardown cost everywhere.\n`Weak`\nregistration gives one thread per process and self-deregistration.\n\n**Write quiescence is the trigger.** An index whose pending count is\nunchanged\nacross `quiet_ticks` has taken no writes (both counters are monotonic\nbetween\ncompactions, so equality *is* absence of writes — no new write-path\ncounter).\nThis is what actually unlocks the fast path: compacting *during* a burst\ndoes\nnot, because the next insert re-disables it. It is also what closes the\nissue's\n**threshold bootstrap gap** — quiescence is size-independent, so a\n600-edge graph\nreaches the frozen path exactly as a 6M-edge one does.\n`should_compact()` is\nunchanged and still bounds delta growth under a burst that never goes\nquiet;\ndeliberately *not* \"fixed\" to fire at `frozen == 0`, which degenerates\nto\ncompacting on every write.\n\n**A duty-cycle limiter and an amortization floor.** After compacting, an\nindex\nwaits `max(min_interval, cost × (100−duty)/duty)` — compaction can\nconsume at\nmost ~10% of one core per index, scaled by its own measured cost. And a\nquiescent\nindex is only compacted when `pending ≥ frozen_edges / 10_000`, so a\ntrickle of\nwrites into a multi-million-edge graph never buys a full rebuild.\n\n**All policy off the hot path.** Neither the read nor the write path\ngains an\ninstruction of policy — in particular no per-read atomic RMW, which\nwould undo\nthe read-scaling work in #3811.\n\n**Default on, opt out** via `AletheiaDBConfig::adjacency` or\n`ALETHEIADB_ADJACENCY_MAINTENANCE=off` (a process-level switch, since\n`AletheiaDB::new()`/`open()` take no config). No-op on wasm32 and under\nMiri.\n\n## Correctness fixes this made necessary\n\n| | Was | Now | Test |\n|---|---|---|---|\n| **Torn reads** | `compact()` retired the delta *before* publishing the\nCSR that absorbed it — a reader in that window got an adjacency list\n**missing** those edges (an **empty** one on a fresh graph) | Publish\nfirst, retire selectively; the entries the two layers briefly share are\nde-duplicated | `explicit_compaction_never_tears_a_concurrent_read` |\n| **Iterator duplicates** | `get_outgoing_edges_iter` walks the layers\ndirectly, so it missed that de-duplication and could emit an edge twice\n| Both iterators apply it | same test (it alternates both read paths) |\n| **Lost edges** | Two concurrent compactions rebuilt from the same CSR;\nthe later publish dropped the other's edges | `compact()` serialized\nwith itself and with `import_frozen_csr` |\n`concurrent_compaction_does_not_lose_edges` |\n| **Permanent duplicates** | A panic mid-publish left both layers\nholding the same entries; `AdjacencyIndex::build` doesn't de-duplicate,\nso the next compaction made it permanent | RAII publish window records\nthe interruption; the next compaction de-duplicates (paid only after a\npanic) | `compaction_recovers_from_an_interrupted_publish` |\n| **Persisted CSR skew** | The two indexes compact independently, so\npersistence could capture an edge in one direction's CSR and not the\nother's — and reconstruction decided *both* deltas from the outgoing set\nalone | Export the pair under both compaction locks; reconstruct each\ndirection from its own CSR |\n`export_csr_pair_captures_both_directions_at_the_same_point` (invariant\ntest — `export_csr_pair` is new here) |\n| **Phantom edges** | A CSR entry whose edge was missing from the\npersisted edge list became an adjacency to node 0 under label 0 |\nDropped on import, offsets rebuilt |\n`import_csr_drops_entries_whose_edge_is_absent_and_rebuilds_offsets` |\n| **Tombstone resurrection** | Retiring a tombstone while a reader still\nheld the pre-compaction CSR resurrected the deleted edge | Tombstones\nretire only once nothing can still be reading that CSR |\n`tombstones_are_not_retired_while_a_reader_holds_the_retired_csr` |\n\nRead order is now a documented contract: counters→frozen on the fast\npath,\ndelta→frozen→publish-window on the merged path.\n\n## Measurement\n\n**Wall clock** (criterion, new `storage_adjacency_read` bench, 2,000\nnodes ×\ndegree 8) — the same call, merged vs frozen:\n\n| | Merged | Frozen | Δ |\n|---|---|---|---|\n| `get_outgoing_edges` | 92.46 ns | 62.48 ns | **−32.4%** |\n\n**Instructions** (callgrind on the issue's own *unmodified* harness,\n`BOLT_NODES=1000 BOLT_OUT_DEGREE=6 BOLT_READ_ITERS=3000`,\n`--fair-sched=yes`,\nmaintenance on vs off in one binary):\n\n| | Off (merged) | On (frozen) | Δ |\n|---|---|---|---|\n| `get_outgoing_edges`, inclusive | 57.41M | 49.17M | **−14.4%** |\n| Whole workload | 358.27M | 359.90M | +0.5% |\n\nTwo things worth stating plainly, since the issue was opened on an\ninstruction\ncount:\n\n- The merged-guard machinery the issue measured does leave the profile\n(≈ −9M:\n`MergedAdjacencyGuard::iter`, `get_adjacency`, the delta `DashMap`\nprobe), but\nthe frozen path's own CSR `binary_search` + slice copy adds most of it\nback, so\nthe *instruction* total is flat to slightly up. The win is in what those\ninstructions cost, not how many there are — hence the 32% wall-clock\nresult.\nThree ON runs: 359.90M / 360.43M / 360.76M, so the +0.5% is consistent,\nnot noise.\n- **Plain callgrind cannot observe this feature.** Without\n`--fair-sched=yes`\nvalgrind's thread serialization starves the worker (~21k instructions\nacross a\nwhole run) and it never compacts. Anyone re-running the issue's exact\ncommand\n  will see no change; that is the measurement, not the code.\n\nAlso surfaced: `get_outgoing_edges_iter` costs ~236 ns against the\n`Vec`-returning call's 62 ns because it re-runs the CSR binary search on\nevery\n`next()`. Pre-existing and out of scope here — I'll file it separately.\n\n## Acceptance criteria\n\nThe issue states requirements rather than a checklist; these are them,\nwith\nevidence.\n\n| # | From the issue | Evidence |\n|---|---|---|\n| 1 | Compaction actually runs for a database built through the public\nAPI | `CurrentStorage::new()` → registers both indexes.\n`background_maintenance_drains_delta_after_write_burst` (600 edges →\n`delta 0 / frozen 600` on its own),\n`background_maintenance_drains_tombstones_after_deletes` |\n| 2 | The documented frozen fast path is actually reached |\n`adjacency_stats().is_fully_compacted()` ⇔ fast path, asserted in 4\ntests; measured 92.5 ns → 62.5 ns |\n| 3 | Lifecycle: no thread per DB, no `Drop`/shutdown obligation, no\nleak | One shared worker;\n`maintenance_does_not_spawn_a_thread_per_database` (differential\n`/proc/self/task`), `dropped_database_is_deregistered`; `Weak` registry\npruned per tick; worker parks when empty |\n| 4 | Threshold bootstrap gap decided, not patched in isolation |\nQuiescence trigger is size-independent;\n`small_graph_below_size_thresholds_still_reaches_the_fast_path` asserts\n`should_compact()` is *false* for that exact state and the DB still\ncompacts. `should_compact()` untouched; rationale + rejected alternative\nin ADR-0060 §2, amortization floor in §6 |\n| 5 | Before/after callgrind on this same harness | Table above; harness\nunmodified; commands in ADR-0060 |\n| 6 | `just check-all` (tests, lint, coverage) | `cargo clippy\n--all-targets --all-features -- -D warnings` clean; `cargo fmt --all`;\n**6,323 tests pass** (`--lib` + all 217 integration binaries,\n`--no-fail-fast`). 3 failures, all environmental and pre-existing: two\nforce I/O errors with `chmod 0o444`/`0o500`, which **root** (this\ncontainer) bypasses; one spawns `aletheia-mcp`, built only with the\n`mcp-server` feature CI enables. Coverage not run locally (`llvm-cov`\nunavailable here); CI gates it |\n| 7 | A decision on default-on vs opt-in vs another trigger | ADR-0060:\ndefault-on shared worker, quiescence-triggered, with four rejected\nalternatives (per-index threads, read-path lazy compaction,\ncommit-boundary compaction, opt-in-only). Config + env opt-outs |\n| 8 | *(implicit)* No correctness regression from making this reachable\n| Seven defects found and fixed, each with a test (per-fix in the table\nabove); reviewed from four angles, then from mutation coverage |\n\n## Review\n\nReviewed by four independent passes (concurrency/memory-ordering,\nAPI/back-compat/docs, test quality/flakiness, performance) before\nopening. Every\nP1 and every actionable P2 is fixed in the final commits; deferred items\n(packing\nthe two read-path counters into one `AtomicU64`, a read-demand signal\nfor\nwrite-only databases, the `_iter` binary-search cost) are recorded in\nADR-0060 or\nwill be filed as issues.\n\n**Mutation coverage caught what those four passes did not.** The\n`Mutants (PR\ndiff)` job hit its 120-minute timeout and was cancelled, so its score\ngate was\nskipped -- but the `if: always()` upload still produced a partial\nartifact, and\nit reported three surviving mutants, all in\n`AdjacencyIndex::import_csr`. The\ndecisive one is `dropped += 1` -> `dropped -= 1`: that underflow-panics\nin a\ndebug build the moment the drop path runs, so its survival proved **no\ntest\nanywhere drove `import_csr` with a missing edge id** -- the phantom-edge\nfix had\nno regression test. Auditing the other six on that basis found two more\nwith no\nred-without-fix test (persisted CSR skew, tombstone resurrection). All\nthree are\ncovered as of the last commit, and the two new regression tests are\nverified red\nagainst a temporary revert of their fixes (`left: 3, right: 2` and\n`left: 0, right: 1`).\n\nTwo mutants are left alive deliberately: `dropped > 0` -> `== 0` and `->\n< 0`\nonly flip whether a diagnostic `eprintln!` fires. No behaviour depends\non it,\nand killing them would mean widening `import_csr`'s signature to report\na count\npurely so a test could assert on a warning line.\n\n---\n_Generated by [Claude\nCode](https://claude.ai/code/session_014ciV46K96V2mAJg91H3C1z)_\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n\n---------\n\nCo-authored-by: Claude <noreply@anthropic.com>",
          "timestamp": "2026-09-01T12:22:37-05:00",
          "tree_id": "3ca2ccc1eb536b3b660c97d939ee7d9cecd06eb8",
          "url": "https://github.com/autumn-foundation/AletheiaDB/commit/c20803a059899972c39fca0ac02ee1d6a6cc0f9c"
        },
        "date": 1788284185097,
        "tool": "customSmallerIsBetter",
        "benches": [
          {
            "name": "target_single_hop/traverse_one_hop",
            "value": 20.920845063999927,
            "unit": "ns"
          },
          {
            "name": "target_time_travel/worst_case_9_deltas",
            "value": 185.06400547839155,
            "unit": "ns"
          },
          {
            "name": "target_time_travel/at_anchor",
            "value": 180.4484290142149,
            "unit": "ns"
          },
          {
            "name": "target_time_travel/with_5_deltas",
            "value": 176.16678978585426,
            "unit": "ns"
          },
          {
            "name": "target_batch_insertion/insert_1000_edges",
            "value": 387703.02840566135,
            "unit": "ns"
          },
          {
            "name": "target_3_hop/traverse_three_hops",
            "value": 178.0176218159095,
            "unit": "ns"
          }
        ]
      }
    ]
  }
}