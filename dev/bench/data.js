{
  "lastUpdate": 1784581189708,
  "repoUrl": "https://github.com/autumn-foundation/AletheiaDB",
  "entries": {
    "AletheiaDB Benchmarks": [
      {
        "commit": {
          "author": {
            "email": "markmasterson@gmail.com",
            "name": "Mark Masterson",
            "username": "madmax983"
          },
          "committer": {
            "email": "noreply@github.com",
            "name": "GitHub",
            "username": "web-flow"
          },
          "distinct": true,
          "id": "f5d50d34d5cf6a894ca4fd1942d5d39c8aafb98a",
          "message": "Add USE/IN NAMESPACE read-scope grammar to the Cypher parser (#3349) (#3749)\n\n## Summary\n\nBrings the **Cypher** query language to parity with the AQL `USE\nNAMESPACE` grammar (the #3349 namespace-isolation follow-up to the AQL\nslice #3736). A Cypher read statement may now carry an optional\nnamespace read-scope prefix that isolates results to one or more agent\nnamespaces, and every path that cannot thread that scope **fails\nclosed** rather than silently widening results across namespaces.\n\n## Before / After\n\n**Before.** Cypher had no in-statement namespace scoping.\n`db.execute_cypher(\"MATCH (n:Person) RETURN n\")` was namespace-agnostic,\nand there was no way to write a namespace restriction directly in a\nCypher statement — only the programmatic `execute_cypher_scoped(query,\nscope)` existed. AQL already had the `USE NAMESPACE` grammar; Cypher did\nnot.\n\n**After.** Cypher accepts the same prefix grammar as AQL, via the\nembedded Rust `execute_cypher` / `execute_cypher_scoped` API:\n\n```cypher\nUSE NAMESPACE 'agent:a'                        -- single namespace\nUSE NAMESPACE 'agent:a', 'shared'              -- union\nUSE ALL NAMESPACES                             -- no filter (every namespace)\nIN NAMESPACE 'agent:a'                          -- IN is a synonym for USE\nIN ALL NAMESPACES\nUSE NAMESPACE 'agent:a' AS OF TIMESTAMP '...'   -- composes with temporal, either order\nUSE NAMESPACE 'agent:a' FOR SYSTEM_TIME AS OF '...'\nUSE NAMESPACE 'agent:a' BETWEEN '...' AND '...'\n```\n\nNames may be a string literal or a bare identifier (qualified names such\nas `agent:a` must be quoted). Omitting the clause reproduces prior\nbehavior **exactly** (`scope: None`, never `All`). `USE` / `NAMESPACE` /\n`NAMESPACES` / `ALL` stay usable as ordinary identifiers, labels,\nvariables, and property keys everywhere else (contextual keywords — no\nlexer change); only `IN` is a reserved token, and it is only a prefix at\nstatement start (the `WHERE ... IN [...]` operator is unaffected).\n\n## How\n\n| File | Change |\n|------|--------|\n| `src/cypher/ast.rs` | New `CypherNamespaceClause { All,\nNames(Vec<String>) }` + `is_restricting()`; new `namespace:\nOption<CypherNamespaceClause>` field on `CypherStatement::Match`. |\n| `src/cypher/parser.rs` | Prefix loop parsing temporal + namespace in\neither order with duplicate rejection; `parse_namespace_clause` /\n`parse_namespace_name`; contextual-keyword helpers\n(`check_contextual_kw` / `at_namespace_start`) mirroring the\nEXPLAIN/PROFILE precedent; `reject_restricting_namespace_on_write`. |\n| `src/cypher/converter.rs` | `convert_namespace_clause` lowers the\nclause into `Query.scope`, reusing AQL's logic (lone-`all` special case,\n`Namespace::new` validation, empty-union guard); `NamespaceError` →\n`CypherError::ParameterError` (INVALID_ARGUMENT). Replaces the `scope:\nNone` marker at the single `Query{}` success site. |\n| `src/cypher/exec.rs` | `statement_namespace()` accessor; fail-closed\nrejection of a restricting clause on the multi-variable evaluator path\nin `plan_cypher_with_params`. |\n| `src/cypher/multi_pattern.rs` | Destructure updated (`namespace: _`).\n|\n| `src/db/query.rs` | EXPLAIN/PROFILE now thread the in-query scope\n(`in_query_scope_or_all`) instead of hardcoding `All`;\n`execute_cypher_execution_scoped` refuses a programmatic scope combined\nwith an in-statement clause. |\n| `src/cypher/mod.rs` | Re-exports. |\n| `src/cypher/tests.rs` | Coverage tests (`namespace_grammar`, 36\ntotal). |\n| `docs/guides/namespaces-guide.md` | New Cypher `USE NAMESPACE`\nsubsection (grammar + v1 limitations/footguns). |\n\n## Design rationale\n\n**Where the scope lives — approaches considered.**\n- *Mirror-AQL, single-`Query` lowering (chosen).* The clause is attached\nonly to `CypherStatement::Match` (parallel to the existing `temporal`\nfield), not to `Write`/`Unwind`. Writes and standalone `UNWIND` reject\nthe prefix at parse time, so they never carry it; EXPLAIN/PROFILE wrap\nan inner `Match` that already carries it. This keeps `converter.rs`'s\nsingle-`Query` lowering the *only* place a clause becomes a\n`NamespaceScope`, so the executor's existing `execute_query`\nvalidate/apply path does the rest with no new plumbing.\n- *Thread the scope through every execution path (rejected).* Would\nduplicate scope-application logic into the multi-pattern evaluator and\neach write path — more surface, more places to forget the guard, no\nuser-visible gain in v1 (those paths reject a restriction anyway).\n- *Wrapper AST node around the whole statement (rejected).* A\n`Scoped(Box<Statement>)` node would touch every match arm across\nparser/converter/exec/multi_pattern and complicate the either-order\ncomposition with `temporal`; the field-on-`Match` approach reuses the\nexact `temporal` precedent.\n\n**Fail-closed reasoning (the security crux).** A namespace restriction\nthat is silently dropped **widens** results and leaks other agents'\nfacts. So on every execution path that cannot thread `Query.scope`, a\n*restricting* clause is a structured error, never an unscoped run:\n- mutation (`CREATE` / `MERGE` / `MATCH ... DELETE`) and standalone\n`UNWIND` → rejected at parse time;\n- the multi-variable pattern evaluator → rejected in `plan_cypher`;\n- omitted clause → lowers to `None` (**never** `Some(All)`), preserving\nthe MCP layer's fail-closed `default`-namespace resolution for an\nomitted scope.\n\n`USE ALL NAMESPACES` (and the bare `all` selector) impose no\nrestriction, so they are a harmless no-op and are allowed on every path\n— `is_restricting()` is the single source of truth for that distinction.\n\n**Two intentional fail-closed divergences from AQL:**\n1. *Programmatic scope × in-query clause → refused.* AQL's\n`execute_aql_scoped` last-wins-overwrites the in-query scope with the\nprogrammatic one, which can *widen* an intended restriction\n(programmatic `All` erasing an in-query `Single`). For this\nsecurity-sensitive feature the Cypher path deliberately diverges: when a\nprogrammatic scope and an in-statement clause are **both** present, the\ncall is **refused** (`UnsupportedFeature`) rather than silently picking\na winner. This never widens, breaks no existing scoped test (pre-feature\nstrings carry no clause), and is pinned by tests. It keys on the\nclause's *presence* (not on whether it narrows), so even a\nnon-restricting `USE ALL NAMESPACES` in-query clause is refused under a\nprogrammatic scope — a conscious \"specify the scope in exactly one\nplace\" contract.\n2. *Bare `all` selector kept for parity.* A lone `all` in the name\nposition (bare or quoted) lowers to `NamespaceScope::All`, identical to\nthe AQL converter and the MCP `namespace:\"all\"` string. Kept for\ncross-surface parity; documented as a footgun (a namespace literally\nnamed `all` can never be created — `Namespace::new(\"all\")` is rejected —\nso `USE NAMESPACE all` has exactly one sensible meaning).\n\n**EXPLAIN/PROFILE.** Previously hardcoded `NamespaceScope::All`. Now the\ninner statement's lowered scope is threaded into planning, so a scoped\n`EXPLAIN` honors the `NOT_FOUND` contract for an unknown namespace, and\na scoped `PROFILE` — which *executes* to populate its per-operator\ncounters — runs under the scope, so its reported row counts reflect the\n*scoped* query rather than leaking an unscoped cross-namespace\ncardinality/timing side channel.\n\n## Acceptance-criteria evidence\n\nDerived from the AQL #3736 parity surface. All tests live in\n`src/cypher/tests.rs::namespace_grammar` (36 total: 27 pre-existing + 9\nadded this pass, plus 2 strengthened).\n\n| Acceptance criterion | Test(s) | Note |\n|---|---|---|\n| Single: `USE NAMESPACE 'x'` → Single(x) isolation |\n`use_namespace_single_isolates` | exact-vector negative assertion |\n| Union: `USE NAMESPACE a, b` → exactly a,b |\n`use_namespace_union_spans_exactly_listed`,\n`union_scope_is_order_independent` | + order-independence (a,b == b,a) |\n| ALL: `USE ALL NAMESPACES` → All | `use_all_namespaces_sees_everything`\n| strengthened to span the **default** namespace |\n| IN synonym | `in_namespace_synonym_isolates_like_use`,\n`in_all_namespaces_is_all_scope` | |\n| Omitted → default (fail-closed) |\n`omitted_namespace_clause_lowers_to_none_not_all`,\n`no_clause_is_unchanged_agnostic_behavior` | lowers to `None`, never\n`Some(All)` |\n| Either-order temporal composition |\n`namespace_before_temporal_parses_both`,\n`temporal_before_namespace_parses_both`,\n`namespace_and_temporal_both_lower_onto_one_query`,\n`namespace_composes_with_between_and_system_time_either_order` | now\ncovers `BETWEEN` + `FOR SYSTEM_TIME`, both orders |\n| Duplicate clause → parse error |\n`duplicate_namespace_clause_is_parse_error` | |\n| Malformed → parse error | `malformed_namespace_clause_is_parse_error`,\n`use_all_without_namespaces_and_plural_without_all_are_parse_errors` |\nadds `USE ALL` (no NAMESPACES) + plural `USE NAMESPACES` |\n| Unknown namespace → NOT_FOUND |\n`unknown_namespace_is_not_found_at_execution`,\n`explain_restricting_unknown_namespace_is_not_found`,\n`profile_restricting_unknown_namespace_is_not_found` | EXPLAIN **and**\nPROFILE now proven to thread scope |\n| Contextual-keyword non-breakage | `use_is_usable_as_a_variable`,\n`namespace_is_usable_as_a_property_key`,\n`all_and_namespaces_are_usable_as_variables_and_labels`,\n`in_operator_in_where_still_parses`,\n`bare_identifier_name_parses_as_single`,\n`contextual_keywords_are_case_insensitive` | + case-insensitivity (`use\nnamespace`, `In All Namespaces`) |\n| Scope-threading regression |\n`restricting_clause_threads_single_scope_onto_query` | fails if\nconverter reverts to `scope: None` |\n| Bare/quoted `all` → All (AQL parity + footgun) |\n`bare_and_quoted_all_name_lowers_to_all_scope` | mirrors AQL\n`convert_bare_all_name_sets_all_scope` |\n| Duplicate name in union un-deduped |\n`duplicate_name_in_union_is_accepted_undeduped` | pins current\n`List([a,a])` behavior |\n| Fail-closed on non-Query paths (multi-pattern) |\n`restricting_clause_on_multi_pattern_is_structured_error`,\n`all_namespaces_on_multi_pattern_is_allowed` | multi-pattern test\nstrengthened to prove `All` spans 3 namespaces (incl. default) |\n| Fail-closed on non-Query paths (mutation) |\n`restricting_clause_on_direct_create_is_structured_error`,\n`restricting_clause_on_match_delete_is_structured_error` | |\n| Fail-closed on non-Query paths (EXPLAIN/PROFILE) |\n`all_namespaces_on_explain_is_allowed`,\n`profile_over_scoped_namespace_reports_scoped_row_counts` | PROFILE\ncounters proven scoped |\n| Programmatic scope × in-query clause → refused |\n`programmatic_scope_plus_in_query_clause_is_rejected`,\n`mcp_default_scope_plus_in_query_clause_is_rejected`,\n`programmatic_scope_without_clause_still_isolates` | pins the exact\n`NamespaceScope::default()` scope the MCP query tool injects |\n\n```\ntest result: ok. 537 passed; 0 failed; 0 ignored; 0 measured; 4452 filtered out\n```\n(`cargo test --features cypher --lib cypher` — full Cypher suite,\nincludes the 36 `namespace_grammar` tests.)\n\n## Adversarial review summary\n\nThree adversarial review lenses were run against the merged\nparser/converter/execution slice:\n\n1. **Security / isolation-leak.** Attempted to construct a query or call\nsequence that sees *more* namespaces than scoped, or silently drops a\n*restricting* clause. **Verdict: no leak.** Every unthreadable path\nfails closed or is provably equivalent to the requested scope;\n`is_restricting()` and the converter agree on the only two\nnon-restricting inputs (`All`, bare `all`).\n2. **Parser non-breakage.** Attempted to find an existing valid-Cypher\nstatement the prefix loop regresses, or a wrong-accept. **Verdict: no\nregression.** No valid Cypher body begins with `USE`/`IN`, so the prefix\nloop can never fire on a legitimate statement; contextual keywords\nremain usable as identifiers/labels/property keys.\n3. **AQL parity / test rigor.** Checked lowering parity and whether\nguard tests actually catch their reverts. **Verdict: lowering is exact;\nisolation tests use exact-vector negative assertions.**\n\n**Findings addressed in this pass (all additive — no behavior change):**\nbare/quoted `all` converter branch now covered (was zero-coverage);\nPROFILE scope-threading now proven (NOT_FOUND + scoped counters); the\nMCP-injected `default` scope collision now pinned; union\norder-independence + un-deduped duplicate name pinned;\ncontextual-keyword case-insensitivity, `BETWEEN`/`FOR SYSTEM_TIME`\ncomposition, and the `USE ALL`/plural-`USE NAMESPACES` parse errors\ncovered; the two weak `USE ALL NAMESPACES` tests strengthened to span\nthe default namespace and prove real multi-namespace spanning. All\nfootguns and the MCP boundary documented in the namespaces guide.\n\n## Disclosed limitations / known-OK\n\n- **MCP `query`-tool + HTTP surfacing is a deferred follow-up.** The MCP\n`query` tool always injects a programmatic scope\n(`parse_opt_scope(...).unwrap_or_default()` = `default`-only when the\n`namespace` arg is omitted) and dispatches through\n`execute_cypher_scoped`; the fail-closed collision guard then refuses\nany in-query clause submitted through MCP. So in v1 the in-query Cypher\nclause is reachable **only** via the embedded Rust API, and MCP callers\nscope with the tool's `namespace` **parameter** instead. This matches\nhow the AQL slice (#3736) and the house pattern ship a grammar slice\nRust-API-first, with MCP surfacing in a separate registry PR (that\nregistry is owned by another lane and is untouched here). Documented in\nthe guide's v1-limitations note.\n- **Bare-`all` and uppercase-`ALL` footguns (inherited from AQL).** `USE\nNAMESPACE all` / `'all'` means all-namespaces; `USE NAMESPACE ALL`\n(uppercase) is instead a *restricting* filter to a namespace named `ALL`\n(the selector is case-sensitive). Prefer `USE ALL NAMESPACES` for \"no\nfilter\". Documented.\n- **Un-deduped union list.** `USE NAMESPACE 'a', 'a'` lowers to\n`List(['a','a'])` (parity with `NamespaceScope::list`); membership still\nresolves correctly. Pinned by test.\n- **Known-OK environment gate caveats (not caused by this change):** the\n2 uid-0 havoc tests, sandbox ENOSPC on the full-workspace test run, and\nrepo-wide coverage/mutants red until #3737 merges.\n\n## Gate status\n\n- `cargo fmt --all --check` — clean.\n- `cargo clippy --lib --features\nconfig-toml,mcp-server,sharding-rpc,simulation,cypher -- -D warnings` —\nclean (0 warnings).\n- `cargo test --features cypher --lib cypher` — 537 passed, 0 failed.\n\n## Scope / non-goals\n\nParser + converter + execution-seam + coverage tests + docs only. The\nMCP `query` tool registry and `src/mcp/server.rs` are untouched (owned\nby another lane); the WAL format is unchanged.\n\n---------\n\nCo-authored-by: Claude <noreply@anthropic.com>",
          "timestamp": "2026-07-20T15:49:00-05:00",
          "tree_id": "ca3d7958274fc770daa79af8a4b013de048cb73a",
          "url": "https://github.com/autumn-foundation/AletheiaDB/commit/f5d50d34d5cf6a894ca4fd1942d5d39c8aafb98a"
        },
        "date": 1784581189707,
        "tool": "customSmallerIsBetter",
        "benches": [
          {
            "name": "target_3_hop/traverse_three_hops",
            "value": 126.15822656980654,
            "unit": "ns"
          },
          {
            "name": "target_single_hop/traverse_one_hop",
            "value": 15.094382243996188,
            "unit": "ns"
          },
          {
            "name": "target_batch_insertion/insert_1000_edges",
            "value": 373134.6829894368,
            "unit": "ns"
          },
          {
            "name": "target_time_travel/worst_case_9_deltas",
            "value": 151.3381127977135,
            "unit": "ns"
          },
          {
            "name": "target_time_travel/at_anchor",
            "value": 147.05760669882358,
            "unit": "ns"
          },
          {
            "name": "target_time_travel/with_5_deltas",
            "value": 145.469580987681,
            "unit": "ns"
          }
        ]
      }
    ]
  }
}