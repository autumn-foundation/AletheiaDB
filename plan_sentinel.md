1. Also, let's de-abstract `SemanticRule` in `sentinel.rs` using an enum. Dynamic dispatch for two simple rules is unnecessary. We can use an enum `SemanticRule` with variants `VectorBan` and `NumericRange`.
   Wait, the user's prompt specifically mentions "De-Abstract: Replace single-implementation Traits with concrete Structs." Since `SemanticRule` has multiple implementations, replacing it with an Enum is not strictly "Replace single-implementation traits", but it might fall under "Flatten" or "Destroy speculative generality". "If you need a diagram to explain the class hierarchy, you failed."
