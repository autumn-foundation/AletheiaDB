**[Weak Test Coverage in Temporal Types Boundaries and Complex Conditions]**
**Module:** `src/core/hlc.rs`
**Summary:** Mutants regarding strict exact math evaluation of constants `MAX_BACKWARD_DRIFT_US` and `MAX_FORWARD_JUMP_US` (`*` mutated to `+` or `/`), specific boundary behaviors when `drift == -MAX_BACKWARD_DRIFT_US`, and exact combinations of logical condition variables `&&` mutating to `||` inside `HybridTimestamp::receive` complex logic survived.
**Diagnosis:** MISSING_TEST / WEAK_TEST - Existing tests tested positive path boundary logic well, but neglected specific exact value boundary tests for mathematical evaluation inside `evaluate_clock_skew`, as well as complex scenarios inside `receive` where only one condition of the `&&` is true, leading a `||` mutation to incorrectly trigger the wrong boundary.
**Kill Shot:** Appended explicit exact boundary mathematical tests (`test_evaluate_clock_skew_exact_boundaries_math`, `test_hybrid_timestamp_receive_complex_conditions`) directly to `tests` mod inside `src/core/hlc.rs`.
