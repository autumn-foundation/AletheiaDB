Great, I will also remove `pub mod onnx;` and `src/embeddings/providers/onnx.rs`.
Actually, "The `embedding-onnx` feature, along with its `tokenizers` and `ort` dependencies, were removed from AletheiaDB".
I can delete `src/embeddings/providers/onnx.rs`.

Let's test removing them.
